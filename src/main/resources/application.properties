spring.application.name=SpringCore
server.port=7777
####### BAZA uchun ##########
spring.datasource.url=jdbc:postgresql://localhost:5432/spring_core
spring.datasource.username=postgres
spring.datasource.password=bexa
spring.datasource.driver-class-name=org.postgresql.Driver
# = JPA / HIBERNATE
# ===============================
# Dialect - PostgreSQL uchun
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Schema strategy: none, validate, update, create, create-drop
spring.jpa.hibernate.ddl-auto=update

# SQL querylarni logga chiqarish
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
springdoc.swagger-ui.path=/swagger-ui
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB